export const ACTION = Object.freeze({
    TRIGGER_SYNCENGINE_ENDPOINT: 'action.syncengine.trigger_endpoint',
});

export const MODAL = Object.freeze({
    TRIGGER_SYNCENGINE_ENDPOINT: 'sw-flow-syncengine-endpoint-modal',
});

export default {
    ACTION,
    MODAL,
};
